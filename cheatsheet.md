# 리눅스 치트시트

## 단축키
Ctrl+C 		중단
Tap		자동완성
위/아래		이전 명령어
Ctrl+R		명령어 검색
Ctrl+shift+C/V	복사/붙여넣기
Ctrl+L		화면 지우기

## 이동
pwd		나 어디 있나
cd ~		홈으로
cd ..		한 칸 위
cd -		직전 위치

## 보기
ls -lh		자세히 + 읽기 쉽게
ls -lt		최근 수정 순
du -h --max-depth=1 폴더 진짜 크기

## 만들기 / 옮기기
mkdir -p 경로	중간 폴더까지
touch 파일 	빈 파일
cp -r 원본 대상	폴더 복사
mv 원본 대상	이동 = 이름변경

## 지우기
rm 파일		영구 삭제 (휴지통 없음)
trash-put 파일	휴지통으로
rm -r 폴더	폴더째

## 습관
rm 전에 같은 대상으로 ls 먼저!

